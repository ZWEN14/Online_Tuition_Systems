@{
    ViewData["Title"] = "Access denied";
}

<div class="text-center py-5">
    <h1 class="display-6">Access denied</h1>
    <p class="text-muted">This temporary account does not have permission to perform that action.</p>
    <a asp-controller="Announcements" asp-action="Index" class="btn btn-primary">Back to announcements</a>
</div>
